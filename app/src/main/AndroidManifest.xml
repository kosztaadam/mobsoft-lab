<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="labor.mobsoft.hu.mobilsoftlab">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".MobSoftApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="DATABASE"
            android:value="mobsoftlab.db" />
        <meta-data
            android:name="VERSION"
            android:value="2" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="labor.mobsoft.hu.mobilsoftlab.model" />

        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.list.ListActivity"
            android:label="Receptek">

        </activity>

        <activity
            android:name=".ui.details.RecipeDetailsActivity"
            android:label="Recept részletei">

        </activity>

        <activity
            android:name=".ui.addrecipe.AddRecipeActivity"
            android:label="Recept hozzáadása">

        </activity>

        <activity
            android:name=".ui.editrecipe.EditRecipeActivity"
            android:label="Recept szerkesztése">

        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="34603f386c6896d4b09af4497f004847b5630d27" />



    </application>

</manifest>